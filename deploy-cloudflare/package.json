{
  "name": "quantecon-deploy-cloudflare",
  "private": true,
  "description": "The wrangler CLI that deploy-cloudflare installs with `npm ci`. Exact pin; Dependabot proposes bumps.",
  "license": "MIT",
  "dependencies": {
    "wrangler": "4.136.3"
  }
}
