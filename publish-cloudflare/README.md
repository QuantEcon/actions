# Publish to Cloudflare Worker (Private)

This composite action deploys a static site to a Cloudflare Worker and
enforces a Cloudflare Access policy that restricts traffic to a specific
GitHub team. It also performs a quick verification that the endpoint is
properly gated.

## Usage

